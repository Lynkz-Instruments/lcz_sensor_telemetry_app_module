---
info:
  title: lcz_sensor_app_attributes
attributes:
  - name: network_id_filter
    summary: If nonzero, then filter advertisements based on network ID
    required: true
    schema:
      type: integer
    x-ctype: uint16_t
    x-default: 0
    x-savable: true
    x-writable: true
    x-readable: true
